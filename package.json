{
  "name": "image-processing-api",
  "version": "1.0.0",
  "description": "Accessing the provided URL with image information should resize an image and save it to disk on first access, then pull from disk on subsequent access attempts",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier-ts": "prettier --config .prettierrc 'src/**/*.ts' --write",
    "prettier-js": "prettier --config .prettierrc 'build/**/*.js' --write",
    "lint-js": "eslint 'build/index.js'",
    "lint-ts": "eslint . --ext .ts",
    "build": "npx tsc"
  },
  "keywords": [],
  "author": "jaguilarweb",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.24",
    "@typescript-eslint/eslint-plugin": "^5.19.0",
    "@typescript-eslint/parser": "^5.19.0",
    "eslint": "^8.13.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-prettier": "^4.0.0",
    "prettier": "^2.6.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
